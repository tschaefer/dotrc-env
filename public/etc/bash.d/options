# append to history
shopt -s histappend

# check window and resize if necessary
shopt -s checkwinsize

# ignore minor spelling errors on 'cd'
shopt -s cdspell

# command is hashed or path search
shopt -s checkhash

# save multiliner as single line in history
shopt -s cmdhist

# extended pattern matching under pathname expansion
shopt -s extglob

# re-edit a failed history substitution (depends on readline)
shopt -s histreedit

# allowing further modification before parsing (depends on readline)
shopt -s histverify

# hostname completion on "@" (depends on readline)
shopt -s hostcomplete

# no command completion on empty line (depends on readline)
shopt -s no_empty_cmd_completion

# replaces directory names with the results of word expansion
[ $BASH_VERSION_MAJOR -ge 4 -a $BASH_VERSION_MINOR -ge 2 ] && {
	shopt -s direxpand
}

# file mode creation mask (owner rwx, group rx, world rx)
umask 022

# emacs command line editing mode
set -o emacs

# vim: set filetype=sh :
