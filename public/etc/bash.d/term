# set term specified settings
case ${TERM} in
	linux)
		unalias vim
		;;
	vt100)
		if [ -x $(type -P resize 2>/dev/null ) ]; then
			resize
		fi
		;;
	xterm*)
		echo -ne "\033]0;${USER}@${HOSTNAME%%.*}\007"
		;;
esac

# vim: set filetype=sh :
